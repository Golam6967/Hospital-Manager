# Root .env — used by Docker Compose for PostgreSQL container credentials
# Copy to .env and fill in values before running docker-compose up

POSTGRES_USER=musabbereen
POSTGRES_PASSWORD=remontada
POSTGRES_DB=Database
